<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="ADHDCompanionApp.AppShell"
    xmlns:views="clr-namespace:ADHDCompanionApp.Views"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.TabBarBackgroundColor="#263746"
    Shell.TabBarTitleColor="#FFFFFF"
    Shell.TabBarUnselectedColor="#D6D6D6"
    Shell.TabBarForegroundColor="#FFFFFF">

    <TabBar x:Name="MainTabBar">

        <ShellContent
            Title="Arlo"
            Route="ArloPage"
            ContentTemplate="{DataTemplate views:ArloPage}" />  

       <!-- <ShellContent
            Title="Today"
            Route="TodayPage"
            ContentTemplate="{DataTemplate views:TodayPage}" />

        <ShellContent
            Title="Support"
            Route="SupportPage"
            ContentTemplate="{DataTemplate views:SupportPage}" />

        <ShellContent
            Title="Progress"
            Route="ProgressPage"
            ContentTemplate="{DataTemplate views:ProgressPage}" /> -->
    </TabBar>
</Shell>